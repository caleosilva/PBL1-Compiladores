// Este eh um comentario de linha

/*
Este eh um comentario de bloco
que se estende por multiplas linhas.
*/

class ExampleClass {
    int variavel = 10;
    float pi = 3.14;  // Comentario de linha apos uma declaracao
    string nome = "Joao";  /* Comentario de bloco inline */

    // Teste de string nao fechada:
    string erroString = "Este eh um erro;

    char caractere = 'a';  // Um caractere valido
    char erroChar = 'ab';  // Erro: mais de um caractere

    if (variavel == 10 && pi != 3.0) {
        // Mais operadores e um número decimal
        variavel = variavel + 1;
    }
    
    // Teste de comentario nao fechado:
    /* Este eh um comentario de bloco nao fechado
}
