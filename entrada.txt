// Este eh um comentario de linha

/*
Este eh um comentario de bloco
que se estende por multiplas linhas.
*/

class ExampleClass {
    variavel = 10;
    pi = 3.14;  // Comentario de linha apos uma declaracao
    nome = "Joao";  /* Comentario de bloco inline */

    // Teste de string nao fechada:
    erroString = "Este eh um erro;

    caractere = 'a';  // Um caractere valido
    erroChar = 'a;  // Erro: Caractere aberto

    if (variavel == 10 && pi != 3.0) {
        // Mais operadores e um número decimal
        variavel = variavel + 1;
    }

    while pi < 3 {
        variavel--
    }
    
    // Teste de comentario nao fechado:
}

/* Este eh um comentario de bloco nao fechado
essas
linhas
serão
consumidas 